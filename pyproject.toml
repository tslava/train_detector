[project]
name = "train-detector"
version = "0.1.0"
description = "Detect passing trains using microphone audio levels"
license = "MIT"
keywords = ["audio", "train", "detection", "sounddevice"]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
    "sounddevice>=0.5.2",
]
